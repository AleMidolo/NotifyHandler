{
  "name": "notify-handler",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@11.19.0",
  "engines": {
    "node": "24.21.x",
    "npm": "11.19.x"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present && node --test scripts/run-live-explorer.test.mjs scripts/portable-live-explorer.test.mjs scripts/portable-live-explorer-sisal.test.mjs scripts/portable-live-explorer-bet365.test.mjs",
    "check": "npm run build && npm run lint && npm run typecheck && npm test",
    "desktop:dev": "npm run dev --workspace @notify-handler/desktop",
    "browser:install": "node ./node_modules/playwright-core/cli.js install chromium",
    "browser:install:ci": "node ./node_modules/playwright-core/cli.js install --with-deps chromium",
    "live:explore:local": "node scripts/run-live-explorer.mjs",
    "live:bundle:build": "node scripts/build-live-validation-bundle.mjs",
    "live:bundle:verify": "node scripts/verify-live-validation-bundle.mjs",
    "release:bundle": "node scripts/build-desktop-release.mjs",
    "release:verify": "node scripts/verify-desktop-release.mjs",
    "release:sbom": "npm sbom --sbom-format cyclonedx > dist/release/notifyhandler-sbom.cdx.json",
    "live:sisal:bundle:build": "node scripts/build-sisal-live-validation-bundle.mjs",
    "live:sisal:bundle:verify": "node scripts/verify-sisal-live-validation-bundle.mjs",
    "live:bet365:bundle:build": "node scripts/build-bet365-live-validation-bundle.mjs",
    "live:bet365:bundle:verify": "node scripts/verify-bet365-live-validation-bundle.mjs"
  },
  "devDependencies": {
    "electron": "44.3.0",
    "esbuild": "0.28.2",
    "typescript": "6.0.3"
  }
}
